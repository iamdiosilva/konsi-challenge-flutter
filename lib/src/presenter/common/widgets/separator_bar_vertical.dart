import 'package:flutter/material.dart';

class SeparatorBarVertical extends StatelessWidget {
  const SeparatorBarVertical({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 2,
      height: 30,
      color: Colors.grey[400],
    );
  }
}
